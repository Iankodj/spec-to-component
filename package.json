{
  "name": "spec-to-component",
  "version": "0.0.0",
  "description": "",
  "type": "module",
  "main": "dsit/index.js",
  "scripts": {
    "dev": " npx vite",
    "build": "rm -rf dist && npx tsc",
    "bundle": "rm -rf dist/bundle && npx esbuild src/index.ts --bundle --minify --sourcemap  --outfile=dist/bundle/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^3.2.2"
  }
}
